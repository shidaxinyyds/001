# Pin every package that torch (CUDA cu121) pulled in, so pip never
# tries to uninstall/upgrade them. The sandbox blocks all file deletes
# (safe-delete FAIL_CLOSED), so any uninstall breaks the install.
setuptools==65.5.0
typing_extensions==4.9.0
filelock==3.29.0
fsspec==2026.4.0
Jinja2==3.1.3
MarkupSafe==3.0.3
networkx==3.4.2
numpy==2.2.6
pillow==12.2.0
sympy==1.13.1
torch==2.5.1+cu121
torchaudio==2.5.1+cu121
torchvision==0.20.1+cu121
