name: auto_vision
description: 实时麻将训练器：通过屏幕录制与机器学习识别牌面，并在悬浮窗中给出弃牌策略建议。
version: 1.5.2+17
publish_to: "none"

environment:
  sdk: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  quiver: ^3.0.0

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.1
  permission_handler: ^11.0.1
  share_plus: ^7.2.1
  # 本地 vendor 的 0.4.5（packages/ 内）：上游版 OverlayService 有多条主线程未捕获
  # NPE（START_STICKY 重启 null-intent、缓存引擎缺失、removeView/detach 无守卫），
  # 同进程崩溃即整 App 闪退；并修了 requestPermission/closeOverlay 永不回复、
  # overlay 回传单订阅流二次 listen 抛错。补丁位置见包内【安全补丁】注释。
  flutter_overlay_window:
    path: packages/flutter_overlay_window
  # 本地设置与调试状态持久化
  shared_preferences: ^2.2.0
  # 卡密授权：联网激活/续签 + 本地短期凭证 HMAC 验签
  # 钉死 1.1.0：CI 的 Flutter 3.13 自带 Dart 3.1，而 http ≥1.2 要求 Dart ≥3.2。
  # 不钉死会在 CI 上 version solving failed（本地新 SDK 感知不到这个坑）。
  http: 1.1.0
  crypto: ^3.0.3
  # chaquopy:
  #   git:
  #     url: https://github.com/heyzec/chaquopy_flutter.git
  #     ref: build-files-patched

  # chaquopy: 0.0.19

dev_dependencies:
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  # To add assets to your application, add an assets section, like this:
  # assets:
  #  - images/a_dot_burr.jpeg
  #  - images/a_dot_ham.jpeg
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.
  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages
  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
